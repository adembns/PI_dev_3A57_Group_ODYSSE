{% extends 'base.html.twig' %}

{% block title %}Hotel index{% endblock %}

{% block body %}
<br>
<br>
    <h1>Hotel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Features</th>
                <th>Name</th>
                <th>Prix</th>
                <th>Location</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hotel in hotels %}
            <tr>
                <td>{{ hotel.id }}</td>
                <td>{{ hotel.features }}</td>
                <td>{{ hotel.name }}</td>
                <td>{{ hotel.prix }}</td>
                <td>{{ hotel.location }}</td>
                <td>
                    <a href="{{ path('app_hotel_show_admin', {'id': hotel.id}) }}">show</a>
                    <a href="{{ path('app_hotel_edit', {'id': hotel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_hotel_new') }}">Create new</a>
{% endblock %}
